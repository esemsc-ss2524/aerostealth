# SPDX-License-Identifier: Apache-2.0
"""Composed gradient d(Cd, sigma_agg)/dtheta through geom -> {cfd, em} vs finite differences."""

import pytest

pytest.skip("composition not wired yet", allow_module_level=True)
