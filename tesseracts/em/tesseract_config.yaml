name: "aerostealth-em"
version: "0.1.0"
description: "Meep 2D FDTD: TF/SF plane-wave scattering, near-to-far RCS, KS-aggregated echo width and shape adjoint."

build_config:
  target_platform: "native"
