method: epsilon_constraint
objective: Cd
constraint: sigma_agg

epsilon:
  points: 10

warm_start: anchor_blend

optimizer:
  name: MMA
  max_iter: 12
  anchor_max_iter: 200
  ftol: 1.0e-6
  step_bound: 0.05
  retries: 2
  jobs: 8
