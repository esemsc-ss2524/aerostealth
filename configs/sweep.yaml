method: epsilon_constraint
objective: Cd
constraint: sigma_agg

epsilon:
  points: 8
  include_anchors: true
  schedule: linear

warm_start: previous

optimizer:
  name: SLSQP
  max_iter: 6
  ftol: 1.0e-6
  step_bound: 0.04

ks_continuation:
  rho_start: 10.0
  rho_end: 60.0

reanchor:
  shape_drift_tol: 0.15
